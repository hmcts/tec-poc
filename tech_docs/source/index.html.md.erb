---
title: TEC API POC
weight: 10
---

# TEC API POC

This is the technical documentation for the TEC API proof of concept: a local sandbox for experimenting with CCD configuration and its effect on Manage Cases.

Start with where TEC sits on the shared HMCTS platform, then use the later pages for architecture, database ownership, and the ExUI Upload batch file navigation work.

## Contents

- [Where TEC sits in HMCTS](/hmcts-context.html) — the wider TEC service, the CFT platform, and the common components this POC uses
- [TEC decentralised CCD architecture](/ccd-architecture.html) — components, case types, create/read flows, and local CFTLib topology
- [State and event model](/state-event-model.html) — hand-edited state diagrams for TEC, TEC_BATCH and TEC_EXCEPTION
- [Next step events matrix (worksheet)](/next-step-events-matrix.html) — fill Desired columns for clerk / LA Next steps by state
- [CFTLib database layout](/cftlib-shared-database.html) — `datastore` vs `tec.ccd` vs TEC business tables
- [Local bin scripts](/local-scripts.html) — inventory of every script under `bin/` (seed, link, stack helpers)
- [Local demo catalogue seed](/demo-catalogue-seed.html) — clear, seed curated cases, regenerate design-docs links
- [ExUI primary navigation — Upload batch file](/exui-navigation.html) — local nav proxy and how to verify Upload batch file
- [ExUI Upload batch file proposal](/exui-manage-batches-plan.html) — architect proposal and acceptance criteria

## Run locally

Start the application and documentation sites together:

```bash
./gradlew bootWithCCD
```

Tech docs preview: [http://localhost:4568](http://localhost:4568). Design docs (blockframes, form elements, and caseworker journeys) run separately on [http://localhost:4567](http://localhost:4567). See the repository README for the full local service list and clerk login.
